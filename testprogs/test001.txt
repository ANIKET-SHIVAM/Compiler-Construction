//Simple parsing test
main 
var x, y;
{
    let x <- 51; 
    let y <- 2 * x;
    if y > x then
    	if y < 100 then
   		let x <- x + 20;
   	else
   		let x <- x + 30;
    	fi;
    else
    	let x <- x + 40;	
    fi;
    let x <- x+5;
    //call OutputNum(y)
}.