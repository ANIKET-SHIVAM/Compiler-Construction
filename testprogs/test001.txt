//Simple parsing test
main 
var x, y;
{
    let x <- 51; 
    let y <- 2 * x;
    if y > x then
   		let x <- x + 20;
   		let y <- y + x;
   	else
   		let x <- x + 30;
    fi;
    if y < 100 then
    	let x <- x + 40;	
    fi;
  
    //call OutputNum(y)
}.